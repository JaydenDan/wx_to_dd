# .env 文件示例
# 请复制此文件为 .env 并修改相应的配置

# --- 微信配置 ---
# 在这里填写您想要监听的微信群聊的room id (可选，如果代码中硬编码了则此处无效，视具体实现而定)
# WECHAT_LISTEN_ROOM_WXID="123456789@chatroom"

# --- 钉钉配置 ---
# 本地模式下：填写入想要发送的钉钉联系人备注名或群名称
DINGTALK_LOCAL_SEND_TARGET="钉钉联系人名称"

# Hook模式下：填写群组ID等信息 (具体视 dd_hook.py 实现而定)
DINGTALK_HOOK_SEND_TARGET="123456"
DINGTALK_HOOK_URL="http://localhost:8889/"

# 发送方式配置
# hook: 使用钉钉Hook发送 (需配置 DINGTALK_HOOK_URL)
# local: 使用本地PC版钉钉模拟发送 (需运行本程序的电脑已登录钉钉)
DINGTALK_SEND_METHOD="local"

# 本地发送模式配置 (仅当 DINGTALK_SEND_METHOD="local" 时有效)
# 0: 正常初始化（程序启动时自动搜索联系人并打开窗口）
# 1: 待机模式（不搜索，假设用户已经手动打开了目标聊天窗口，程序直接发送）
DINGTALK_STANDBY="0"

# 系统环境配置
# dev: 开发环境 (输出更多日志，可能开启 headless=False 等调试选项)
# prod: 生产环境
SYSTEM_ENV="prod"

# 日志级别
# DEBUG, INFO, WARNING, ERROR
LOG_LEVEL="INFO"
